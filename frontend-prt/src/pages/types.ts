// types.ts// types.ts (or a similar file)
export interface Blog {
    id: number;         // Use the correct type for your ID
    title: string;
    image: string;
    post: string;
    category: string;
    created_at: string; // Adjust to Date if you want to work with Date objects
}
